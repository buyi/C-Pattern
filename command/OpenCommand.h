#include "Command.h"
class OpenCommand : public Command
{
public:
	// OpenCommand(Application*);
	OpenCommand();
	virtual void Execute ();
	// ~OpenCommand();
// private:
// 	Application* _application;
	
};